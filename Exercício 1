<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Saudação Personalizada</title>
  <style>
    body {
      background: linear-gradient(135deg, #e0f7fa, #ffffff);
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      display: grid;
      place-items: center;
      height: 100vh;
      margin: 0;
    }

    main {
      background: #fff;
      padding: 25px 40px;
      border-radius: 10px;
      box-shadow: 0 3px 12px rgba(0, 0, 0, 0.15);
      text-align: center;
      width: 320px;
    }

    h1 {
      color: #00796b;
      margin-bottom: 15px;
    }

    label {
      display: block;
      font-weight: 600;
      margin-bottom: 8px;
    }

    input {
      width: 90%;
      padding: 8px;
      border: 1px solid #bbb;
      border-radius: 6px;
      font-size: 15px;
      outline: none;
      transition: border-color 0.2s;
    }

    input:focus {
      border-color: #009688;
    }

    button {
      margin-top: 15px;
      padding: 10px 22px;
      background-color: #009688;
      color: white;
      font-size: 15px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      transition: background-color 0.2s;
    }

    button:hover {
      background-color: #00796b;
    }

    #resultado {
      margin-top: 18px;
      font-size: 17px;
      font-weight: 500;
      color: #333;
      min-height: 24px;
    }
  </style>
</head>
<body>
  <main>
    <h1>Olá!</h1>
    <label for="nomeInput">Qual é o seu nome?</label>
    <input type="text" id="nomeInput" placeholder="Digite aqui...">
    <br>
    <button id="btnSaudar">Mostrar Saudação</button>
    <p id="resultado"></p>
  </main>

  <script>
    const botao = document.getElementById("btnSaudar");
    const campoNome = document.getElementById("nomeInput");
    const saida = document.getElementById("resultado");

    botao.addEventListener("click", function() {
      const nome = campoNome.value.trim();

      if (!nome) {
        saida.textContent = "⚠️ Você precisa digitar um nome.";
        saida.style.color = "red";
      } else {
        saida.textContent = `👋 Seja bem-vindo(a), ${nome}!`;
        saida.style.color = "#00796b";
      }
    });

    // Permite enviar com Enter
    campoNome.addEventListener("keypress", function(e) {
      if (e.key === "Enter") botao.click();
    });
  </script>
</body>
</html>


